#! /usr/bin/env python3

import argparse

import bbc.bbc as m

if __name__ == '__main__':
    print('Welcome to The Budge-Board Charts. {}'.format(m.__version__))
    #parser = argparse.ArgumentParser()
    #parser.add_argument('')
    for i, (name, year, info) in enumerate(m.imdb250fileiter(), 1):
        print('{:3} {} {} :: {}'.format(i, year, name, info))
